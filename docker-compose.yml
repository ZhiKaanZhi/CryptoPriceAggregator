version: '3.4'

services:
  crypto-price-api:
    build: .
    ports:
      - "5000:80"
      - "5001:5001"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - DOTNET_USE_POLLING_FILE_WATCHER=1
